{
  "name": "opportunityhunter",
  "version": "1.0.0",
  "description": "OpportunityHunter (DARKMATTER) — multi-app AI agent for opportunity intelligence",
  "type": "module",
  "scripts": {
    "setup": "npm --prefix web install && npm --prefix web run build",
    "start": "node server/index.mjs",
    "dev:web": "npm --prefix web run dev",
    "test": "node --test tests/reliability.test.mjs"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
